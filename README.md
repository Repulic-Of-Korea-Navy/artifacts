# artifacts

bot sort 결과 다운
- gsutil -m cp -r gs://maicon-abc/bot-sort/logs

bot sort 결과 다운
- gsutil -m cp -r gs://maicon-abc/bot-sort/logs

bot sort 결과 다운
- gsutil -m cp -r gs://maicon-abc/bot-sort/logs

bot sort 결과 다운
- gsutil -m cp -r gs://maicon-abc/bot-sort/logs

bot sort 결과 다운
- gsutil -m cp -r gs://maicon-abc/bot-sort/logs

bot sort 결과 다운
- gsutil -m cp -r gs://maicon-abc/bot-sort/logs
